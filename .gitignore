target/
**/*.rs.bk
.DS_Store
.vscode/
.idea/
node_modules/
.env
.env.local
*.so
test-ledger/
# SDK build artifacts
sdk/dist/
sdk/*.tsbuildinfo
# Local Solana keypairs / wallets — never commit
*-keypair.json
*.kp
# QEDGen-bundled Lean library + Lake build artefacts — regenerable from
# `qedgen codegen` so we don't commit the multi-MB tree to history.
formal_verification/*/lean_solana/
formal_verification/*/.lake/
formal_verification/*/build/
# lakefile.lean and lean-toolchain ARE committed (small, version-pinned,
# needed by CI to compile the Lean proofs).
lake-manifest.json
.qed/
