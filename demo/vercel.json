{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd ../sdk && pnpm install --frozen-lockfile=false && pnpm build && cd ../demo && pnpm install --frozen-lockfile=false",
  "buildCommand": "next build",
  "outputDirectory": ".next"
}
